apiVersion: v1
kind: Service
metadata:
  name: python-engine
  labels:
    app: python-engine
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: python-engine
