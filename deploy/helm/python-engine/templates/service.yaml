apiVersion: v1
kind: Service
metadata:
  name: {{ include "minicc-python-engine.fullname" . }}
  labels:
    {{- include "minicc-python-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "minicc-python-engine.selectorLabels" . | nindent 4 }}
