apiVersion: v1
kind: Service
metadata:
  name: {{ include "minicc-gateway.fullname" . }}
  labels:
    {{- include "minicc-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minicc-gateway.selectorLabels" . | nindent 4 }}
